<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.gyroscope.accelerometer"
    android:versionCode="1"
    android:versionName="1.1" >

    <uses-sdk
        android:maxSdkVersion="19"
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar" >
        <activity android:name="app.gyroscope.accelerometer.MainFragmentActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".ActivityB" > -->
        <!-- </activity> -->
        <!-- <activity android:name=".ActivityC" > -->
        <!-- </activity> -->
        <activity
            android:name="app.gyroscope.accelerometer.CollectionDemoActivity"
            android:label="@string/demo_collection" />
        <activity
            android:name="app.gyroscope.accelerometer.LearnedActivityCollection"
            android:label="@string/demo_collection" />
        <!-- <activity -->
        <!-- android:name="app.gyroscope.accelerometer.DataStreamActivity" -->
        <!-- android:label="@string/hello" /> -->
    </application>

</manifest>